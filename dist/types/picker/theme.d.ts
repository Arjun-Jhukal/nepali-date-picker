import type { PickerTheme } from './options';
/**
 * Applies CSS custom properties to the popover element for the given theme.
 */
export declare function applyTheme(el: HTMLElement, theme: PickerTheme): void;
