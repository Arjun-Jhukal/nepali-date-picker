import type { PickerPosition } from './options';
/**
 * Positions the popover element relative to the input element.
 * Flips above/below based on available viewport space.
 */
export declare function positionPopover(popover: HTMLElement, input: HTMLElement, position: PickerPosition): void;
